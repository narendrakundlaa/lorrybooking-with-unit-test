import { NgbPaginationNumber } from '@ng-bootstrap/ng-bootstrap';

export class Truck {
    id: number;
    title: string;
    price: number;
    image: string;
    capacity: string;
    size: string;
}
